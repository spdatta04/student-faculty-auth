<!DOCTYPE html>
<html lang="en">
<head>
    <title>Faculty Registration</title>
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #ff8a65, #4fc3f7); /* Light red to light blue gradient */
            color: #fff;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #ffeb3b; /* Light yellow heading */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        form {
            background-color: rgba(255, 255, 255, 0.2); /* Semi-transparent white form */
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
            width: 350px;
        }

        input[type="text"],
        input[type="password"] {
            width: calc(100% - 24px);
            padding: 12px;
            margin-bottom: 20px;
            border: 2px solid #a1887f; /* Brown border */
            border-radius: 8px;
            box-sizing: border-box;
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.1); /* Semi-transparent input background */
            color: #fff;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            outline: none;
            border-color: #d7ccc8; /* Light brown focus */
            box-shadow: 0 0 10px rgba(215, 204, 200, 0.5);
        }

        button {
            background-color: #ba68c8; /* Light purple button */
            color: #fff;
            padding: 14px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #ab47bc; /* Darker purple button */
        }

        ::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }
    </style>
</head>
<body>
    <h1>Faculty Registration</h1>
    <form action="/faculty/register" method="POST">
        <input type="text" name="name" placeholder="Full Name" required>
        <input type="text" name="phone" placeholder="Phone Number" required>
        <input type="text" name="subject" placeholder="Subject" required>
        <input type="text" name="collegeName" placeholder="College Name" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Register</button>
    </form>
    
</body>
</html>
