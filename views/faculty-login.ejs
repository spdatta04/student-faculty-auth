<!DOCTYPE html>
<html lang="en">
<head>
    <title>Faculty Login</title>
    <style>
        body {
    font-family: 'Roboto', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    background: linear-gradient(135deg, #ff6b6b, #4a90e2); /* Vibrant gradient background */
    color: #fff;
}

h1 {
    text-align: center;
    margin-bottom: 30px;
    color: #ffd700; /* Gold heading */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
}

form {
    background-color: rgba(255, 255, 255, 0.2); /* Semi-transparent white form */
    padding: 30px;
    border-radius: 12px;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
    width: 350px;
}

input[type="text"],
input[type="password"] {
    width: calc(100% - 24px);
    padding: 12px;
    margin-bottom: 20px;
    border: 2px solid #ffc107; /* Amber border */
    border-radius: 8px;
    box-sizing: border-box;
    font-size: 16px;
    background-color: rgba(255, 255, 255, 0.1); /* Semi-transparent input background */
    color: #fff;
}

input[type="text"]:focus,
input[type="password"]:focus {
    outline: none;
    border-color: #ffeb3b; /* Light Amber focus */
    box-shadow: 0 0 10px rgba(255, 235, 59, 0.5);
}

button {
    background-color: #66bb6a; /* Light Green button */
    color: #fff;
    padding: 14px 20px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    width: 100%;
    font-size: 16px;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #4caf50; /* Darker Green button */
}

::placeholder {
    color: rgba(255, 255, 255, 0.7); /* Light placeholder */
}
    </style>
</head>
<body>
    <h1>Faculty Login</h1>
    <form action="/faculty/login" method="POST">
        <input type="text" name="facultyCode" placeholder="Faculty Code" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
</body>
</html>
