<!DOCTYPE html>
<html lang="en">
<head>
    <title>Student Login</title>
    <style>
       body {
            font-family: 'Montserrat', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #8e24aa, #1e88e5); /* Purple to Blue gradient */
            color: #fff;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #ffca28; /* Amber heading */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        form {
            background-color: rgba(255, 255, 255, 0.15); /* Slightly transparent white form */
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
            width: 350px;
        }

        input[type="text"],
        input[type="password"] {
            width: calc(100% - 24px);
            padding: 12px;
            margin-bottom: 20px;
            border: 2px solid #00bcd4; /* Cyan border */
            border-radius: 8px;
            box-sizing: border-box;
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.05); /* Very slightly transparent input background */
            color: #fff;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            outline: none;
            border-color: #4dd0e1; /* Light Cyan focus */
            box-shadow: 0 0 10px rgba(77, 208, 225, 0.5);
        }

        button {
            background-color: #ff5722; /* Deep Orange button */
            color: #fff;
            padding: 14px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #e64a19; /* Darker Deep Orange button */
        }

        ::placeholder {
            color: rgba(255, 255, 255, 0.6);
        } 
    </style>
</head>
<body>
    <h1>Student Login</h1>
    <form action="/student/login" method="POST">
        <input type="text" name="regNo" placeholder="Registration No" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
</body>
</html>
